{
  "name": "chrome-extension-word-counter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bash build.sh",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
